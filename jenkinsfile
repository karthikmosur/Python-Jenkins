pipeline {
  agent { label 'ec2' }

  stages {
    stage('EC2 Agent Test') {
      steps {
        sh '''
          whoami
          hostname
          docker run hello-world
        '''
      }
    }
  }
}
