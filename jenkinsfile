pipeline {
  agent { label 'ec2' }

  triggers {
    pollSCM('H/2 * * * *')  // every 2 minutes
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Run Python') {
      steps {
        sh 'python3 app.py'
      }
    }
  }
}
